#include "platform.nddl"
#include "navigator.nddl"
#include "platform.rules.nddl"
#include "navigator.rules.nddl"

EstimatedState estate = new EstimatedState(Observe);
PlanControlState pcstate = new PlanControlState(Observe);
Gps gps = new Gps(Observe);
OperationalLimits oplimits = new OperationalLimits(Observe);

Navigator navigator = new Navigator(Internal);

Reference reference = new Reference(External);
close();

rejectable (navigator.At target);
target.latitude == to_rad(41.1845);
target.longitude == to_rad(-8.706594);
target.z.specify(2);
target.secs.specify(60);

rejectable (navigator.At target2);
target2.latitude == to_rad(41.184572);
target2.longitude == to_rad(-8.705328);
target2.z.specify(-1);
target2.secs.specify(100);

target after target2;

