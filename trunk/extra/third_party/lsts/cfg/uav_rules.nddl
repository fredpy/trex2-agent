#include "TREX.nddl"
#include "navigator.nddl"
#include "platform.nddl"

Navigator::Visit{
	//met_by(Idle i);

	contains(effect Reference.Going s);
	s.latitude == latitude;
	s.longitude == longitude;
	s.radius == -100.0;
}


//Reference::Surveil{
	//contained_by(Navigator.Visit s);
	//starts_after(ReferenceState.Going arrival);
	  //arrival.near_xy == true;
//}

