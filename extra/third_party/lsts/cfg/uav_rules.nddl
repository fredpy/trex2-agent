#include "TREX.nddl"
#include "navigator.nddl"
#include "platform.nddl"

Navigator::Visit{
	met_by(Idle i);

	contains(effect Reference.Surveil s);
	s.latitude == latitude;
	s.longitude == longitude;
	
}


