#include "platform.nddl"
#include "navigator.nddl"

Reference::Going {
	meets(At a);
	a.latitude == latitude;
	a.longitude == longitude;
	a.z == z;
	a.speed == speed;
}

Reference::YoYo {
	meets(At a);
	a.latitude == latitude;
	a.longitude == longitude;	
	a.z == z;
	a.speed == speed;
}