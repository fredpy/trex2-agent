#include "TREX.nddl"
#include "navigator.nddl"
#include "platform.nddl"

Navigator::Visit{
	//met_by(Idle i);

	meets(effect Reference.Surveil s);
	s.latitude == latitude;
	s.longitude == longitude;
	s.radius == -100.0;
}


//Reference::Surveil{
	//meets(effect Reference.Surveil l2);
	  //l2.radius == radius + 50.0;
	  
//}

